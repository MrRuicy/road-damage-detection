# FastAPI 及相关依赖
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pydantic==2.10.1
pydantic-settings==2.6.1

# YOLO 和 CV
ultralytics>=8.0.20
opencv-python-headless>=4.5.5
pillow>=9.5.0
numpy>=1.22.0

# ONNX 推理（高精确模型为 ONNX 格式，缺失会触发容器内 AutoUpdate 导致卡死）
onnx>=1.16.0
onnxruntime>=1.18.0

# 视频目标追踪（ByteTrack 去重计数）；缺失会触发容器内 AutoUpdate 导致卡死
lapx>=0.5.0

# 视频转码（H.264，浏览器可播放）；容器优先用系统 ffmpeg，缺失时回退此包自带二进制
imageio-ffmpeg>=0.5.0

# 系统监控
psutil>=5.9.0
gputil>=1.4.0

# PDF 生成
reportlab>=3.6.0

# 数据库
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Excel 导出
openpyxl>=3.1.0

# 其他工具
python-dotenv==1.0.1
aiofiles==24.1.0
